@model IList<BrowsePhotoSumaryModel>
@if (Model.Count == 0) { 
    @:(no photos)
} else {
    <table class="support_table">
        @for (int summaryIndex = 0; summaryIndex < Model.Count; summaryIndex++) {
            @Html.DisplayFor(m => m[summaryIndex])
        }
    </table>
}

