@model TMD.Model.Distance
@if (!string.IsNullOrEmpty(ViewData.ModelMetadata.DisplayFormatString)) {
    @Model.ToString(UserSession.Units, renderMode: ViewData.ModelMetadata.DisplayFormatString.ParseEnum(UnitRenderMode.Default))
} else {
    @Model.ToString(UserSession.Units)
}