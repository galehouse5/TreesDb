@model TMD.Model.Coordinates
@if (!string.IsNullOrEmpty(ViewData.ModelMetadata.DisplayFormatString)) {
    @Model.ToString(ViewData.ModelMetadata.DisplayFormatString.ParseEnum(CoordinatesFormat.Default))
} else {
    @Model.ToString()
}