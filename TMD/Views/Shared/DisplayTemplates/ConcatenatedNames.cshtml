@model IList<string>
@if (Model.Count == 1) {
    @Model[0]
}
else if (Model.Count == 2) {
    @Model[0] @:and @Model[1]
}
else {
    for (int i = 0; i < Model.Count - 1; i++) {
        @Model[i]@:,
    }
    @:and @Model[Model.Count - 1]
}