@model Coordinates

@{
    if (ViewData.ModelMetadata.IsReadOnly)
    {
      ViewBag.disabled = true;
    }

    ViewBag.@class = ViewBag.@class == null ? "text" : ViewBag.@class + " text";
    ViewBag.size = ViewBag.size ?? "35";
}

@Html.TextBoxFor(m => m, ViewData)